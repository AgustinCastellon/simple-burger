<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hamburguesas - Simple Burger</title>
    <!--------- GOOGLE FONTS--------->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
    <!----------STYLES----------->
    <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
    <%- include('./partials/header.ejs')%>
    <div class="titles">
        <h1>Menú</h1>
        <p>Las mejores hamburguesas, hechas con ingredientes frescos</p>
    </div>
    <div class="products-box">
    <% productos.forEach((prod) =>{ %>
        <a href="/products/detail/<%= prod.id %>">
            <img src="/img/<%=prod.image %>">
            <article class="articulo">
                <h2><%=prod.name %></h2>
                <div class="descripcion-precio">
                    <p><%= prod.description %></p>
                    <h2 class="precio"><i class="fas fa-dollar-sign"></i><%=prod.price%></h2>
                </div>
            </article>
        </a> 
    <% }) %>     
    </div>   
    
</body>
</html>